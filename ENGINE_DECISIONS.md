# Engine Decisions

## 2026-07-02: `ssaju` 비교 엔진 도입

방향:

- `manseryeok`는 기준 명식 계산 엔진으로 유지한다.
- `ssaju`는 비교/보강 엔진으로 사용한다.
- 서비스 화면에는 바로 `ssaju` 결과를 노출하지 않고, 내부 비교 리포트로 차이를 확인한다.

비교 명령:

```bash
npm.cmd run compare:ssaju
```

대표 입력:

- 2001-08-03 13:30 남성, 양력

현재 일치 항목:

- 원국: 신사년, 을미월, 무술일, 기미시
- 십성
- 지장간
- 12운성
- 공망
- 대운 시작 나이와 대운 간지

현재 차이 항목:

- 특수 신살/길성: 우리 엔진은 포스텔러 화면에 가까운 넓은 표시를 목표로 하여 금여성, 태극귀인, 천문성 등을 더 많이 감지한다. `ssaju`는 해당 예시에서 천을귀인, 화개 위주로 보수적으로 표시한다.
- 관계 탐지: `ssaju`는 `미술 형`과 방향별 관계를 더 자세히 표시한다. 우리 엔진은 현재 중복을 줄인 요약 형태다.
- 천간충: `ssaju`는 `기을 충`, `을신 충`을 모두 표시한다. 우리 엔진은 현재 `을신 충`만 표시한다.

다음 결정 과제:

- 포스텔러식 화면을 우선할지, `ssaju`식 보수 표시를 우선할지 항목별 기준을 나눈다.
- 신살/길성은 `포스텔러 호환 표시`와 `보수 기준 표시`를 분리할 수 있다.
- 관계 탐지는 요약 목록과 상세 목록을 분리한다.
